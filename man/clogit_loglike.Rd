% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clogit.R
\name{clogit_loglike}
\alias{clogit_loglike}
\title{Conditional Logit Log-likelihood}
\usage{
clogit_loglike(y, x, beta, n1 = sum(y))
}
\arguments{
\item{y}{Binary vector of length \code{n}. Response.}

\item{x}{Numeric matrix of size \verb{n x k}. Features.}

\item{beta}{Numeric vector of size \code{k}. Coefficients.}

\item{n1}{Integer scalar. Number of positive cases in the stratum. Defaults
to \code{sum(y)}, which is what conditional logit conditions on.}
}
\value{
The log-likelihood of that event.
}
\description{
Computes the conditional (fixed-effects) log-likelihood contribution of a
single matched set (stratum). Strata that are uninformative -- those in
which every unit acted, or none did -- contribute exactly zero and are
returned as such.
}
\details{
The likelihood conditions on the number of positive outcomes in the
stratum, so the denominator sums over all \code{choose(n, n1)} ways of assigning
\code{n1} positives among the \code{n} units. This is exact, and therefore only
practical for small strata -- the cost grows combinatorially with \code{n}.
}
\examples{
x <- cbind(c(0, 1))
y <- c(1, 0)

# Equivalent to a two-unit conditional logit contribution
clogit_loglike(y, x, beta = 0.7)

# Uninformative strata contribute nothing
clogit_loglike(c(1, 1), x, beta = 0.7)

}
\seealso{
\code{\link[=clogit_perm]{clogit_perm()}} for the model-fitting interface.
}

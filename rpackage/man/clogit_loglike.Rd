% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clogit.R
\name{clogit_loglike}
\alias{clogit_loglike}
\title{Conditional Logit Log-likelihood}
\usage{
clogit_loglike(y, x, beta, n1 = sum(y))
}
\arguments{
\item{y}{Binary vector of length \code{n}. Response (1 = case, e.g. fired the
weapon).}

\item{x}{Numeric matrix of size \verb{n x k}. Features.}

\item{beta}{Numeric vector of size \code{k}. Coefficients.}

\item{n1}{Integer. Number of cases in the stratum. Defaults to \code{sum(y)}.}
}
\value{
The log-likelihood of that single matched set. Strata with no
variation in \code{y} (all cases or all controls) are uninformative and
contribute \code{0} to the conditional log-likelihood.
}
\description{
Computes the exact conditional log-likelihood for a single matched set
(stratum) by brute-force enumeration of all \code{choose(n, n1)} case/control
assignments. Useful as a reference implementation to validate
\code{\link[survival:clogit]{survival::clogit()}} and \code{\link[=clogit_perm]{clogit_perm()}} on small strata.
}
\examples{
set.seed(1)
x <- cbind(rnorm(4))
y <- c(1, 0, 0, 0)
clogit_loglike(y, x, beta = 0.5)

}
